<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="IOT_DeviceManager.APP.Views.DeviceDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Title}"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackLayout
            BackgroundColor="#00B4DB"
            HorizontalOptions="FillAndExpand"
            Orientation="Horizontal"
            VerticalOptions="Fill">
            <Label
                Margin="10,18,0,0"
                d:Text="Device name"
                FontSize="Large"
                HorizontalOptions="FillAndExpand"
                Text="{Binding Device.DeviceName}"
                TextColor="White"
                VerticalTextAlignment="Center" />
            <Label
                Margin="0,18,10,0"
                FontSize="Small"
                HorizontalTextAlignment="End"
                Text="Edit"
                TextColor="White"
                VerticalTextAlignment="Center" />
        </StackLayout>
        <Grid Grid.Row="1" BackgroundColor="#FAFAFA">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                FontSize="Large"
                Text="TestLabel"
                TextColor="Black" />
        </Grid>
    </Grid>

    <!--<StackLayout Padding="15" Orientation="Vertical">
        <StackLayout BackgroundColor="#00B4DB">
            <Label
                d:Text="Device name"
                FontSize="Small"
                Text="{Binding Device.DeviceName}"
                TextColor="White" />
        </StackLayout>
        <FlexLayout BackgroundColor="#FAFAFA" Direction="Column">
            <StackLayout Orientation="Horizontal">
                <Label
                    FontSize="Medium"
                    Text="DeviceType"
                    TextColor="#0083B0" />
                <Label
                    d:Text="Device type"
                    FontSize="Small"
                    HorizontalTextAlignment="End"
                    Text="{Binding Device.DeviceType}"
                    TextColor="#6D6D6D"
                    VerticalTextAlignment="Center" />
            </StackLayout>
        </FlexLayout>

    </StackLayout>-->

</ContentPage>